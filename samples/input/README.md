# Sample Input

Run the following command after installing backend dependencies:

```bash
python3.12 scripts/create_sample_docx.py
```

It creates `samples/input/demo_thesis.docx`, a minimal DOCX thesis used for manual demos.
